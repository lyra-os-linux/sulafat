name: Contracts

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
        with:
          components: rustfmt, clippy
      - name: Install GTK, VTE and packaging validators
        run: sudo apt-get update && sudo apt-get install -y libgtk-4-dev libadwaita-1-dev libvte-2.91-gtk4-dev gettext appstream-util desktop-file-utils rpm
      - name: Test SSH configuration and persistence contracts
        run: cargo test --workspace --locked
      - name: Lint
        run: |
          cargo fmt --all --check
          cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
      - name: Validate translations, desktop metadata and RPM spec
        run: |
          python3 scripts/check-i18n.py
          desktop-file-validate data/org.lyraos.Sulafat.desktop
          appstream-util validate-relax --nonet data/org.lyraos.Sulafat.metainfo.xml
          rpmspec -P packaging/sulafat.spec >/dev/null
